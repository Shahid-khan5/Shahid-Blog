# Wait 15 days before installing newly published versions (supply chain protection).
minimumReleaseAge: 21600
minimumReleaseAgeStrict: false

# esbuild ships its binary through optional dependencies, so its install script is not needed.
allowBuilds:
  esbuild: false
